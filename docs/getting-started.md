# Getting Started

1. Install the extension from the Visual Studio Marketplace or load the `.vsix`.
2. Open a folder containing B4X sources or a `.b4a` project file.
3. Open a `.bas` or `.b4x` file to trigger indexing and IntelliSense.

If you compile locally and run in Extension Development Host (`F5`), open the same file types to test features locally.
